//-
  options.text
  options.isDisabled - bool

mixin simple-button(options = {})
  - const {text = 'Button', isDisabled = false} = options

  button.simple-button(class= isDisabled ? 'simple-button_disabled' : '')= text

