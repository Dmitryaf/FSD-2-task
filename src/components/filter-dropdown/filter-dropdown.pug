include ../text-field/text-field
include ../calendar/calendar

//-
  options.title

mixin filter-dropdown(options = {})
  - const {title = 'filter date dropdown'} = options

  .filter-dropdown
    +calendar({type: 'filter'})
    .filter-dropdown__item
      +text-field({placeholder: 'ДД.ММ.ГГГГ', isLabel: true, title})
